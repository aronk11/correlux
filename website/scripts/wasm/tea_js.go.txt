//go:build js && wasm

package tea

// The browser demo calls Model.Update/View directly. It never starts Program
// or accesses a TTY. Supply only the platform hooks absent upstream on js.
func (p *Program) initInput() error { return nil }
func (p *Program) listenForResize(done chan struct{}) { close(done) }
const suspendSupported = false
func suspendProcess() {}
